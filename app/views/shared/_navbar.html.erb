<nav class="navbar" role="navigation" aria-label="main navigation">
  <div class="navbar-brand">
    <a class="navbar-item event-name" href="/">
      事件記錄簿
    </a>

    <a role="button" class="navbar-burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
    </a>
  </div>

  <div id="navbarBasicExample" class="navbar-menu">
    <div class="navbar-start">
    </div>

    <div class="navbar-end">
      <div class="navbar-item">
        <div class="dropdown is-right is-hoverable">
          <div class="dropdown-trigger">
            <button class="button" style="border:none" aria-haspopup="true" aria-controls="dropdown-menu6">
              <% if user_signed_in? %>
                <span><%= image_tag current_user.avatar.variant(resize:'60x60'), class:"user-avatar is-pulled-right" if current_user.avatar.attached? %></span>
              <% end %>
              <span class="icon is-small">
                <i class="fas fa-angle-down" aria-hidden="true"></i>
              </span>
            </button>
          </div>
          <div class="dropdown-menu" id="dropdown-menu6" role="menu">
            <div class="dropdown-content ">
              <div class="dropdown-item dropdownFix">
                <% if user_signed_in?  %>
                  <%= link_to "個人資料", edit_user_registration_path, class:"navbar-item"  %>
                  <hr class="navbar-divider">
                  <%= link_to '登出', destroy_user_session_path, method: :delete, class:'navbar-item' %>
                <% else %>
                  <%= link_to '註冊', new_user_registration_path, class:'button is-primary is-small' %>
                  <%= link_to '登入', new_user_session_path, class:'button is-light is-small' %>
                <% end %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</nav>